package com.example.fitmvp.presenter;

import com.example.fitmvp.view.activity.LoginActivity;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.junit.MockitoJUnitRunner;

import static org.junit.Assert.*;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

@RunWith(MockitoJUnitRunner.class)
public class LoginPresenterTest {
    @Test
    public void checkNullTest(){
//        LoginActivity loginActivity = mock(LoginActivity.class);
//        when(loginActivity.getAccount()).thenReturn("101");
//        when(loginActivity.getPassword()).thenReturn("a");
//        LoginPresenter loginPresenter = new LoginPresenter();
//        assertFalse(loginPresenter.checkNull());
//        when(loginActivity.getAccount()).thenReturn("");
//        assertTrue(loginPresenter.checkNull());
    }
}